machine:
  ruby:
    version: 2.3.1

deployment:
  production:
    branch: master
    heroku:
      appname: ninegan

dependencies:
  override:
    - bundle install

test:
  override:
    - echo "no test"
